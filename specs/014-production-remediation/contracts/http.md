# HTTP

Credit/vendor-credit lines accept `invoice_line_id` / `bill_line_id`, `quantity`, `price_only`.
Trial balance items add `opening_debit`, `opening_credit`, `closing_debit`, `closing_credit` (existing period `debit`/`credit` retained).
Bank import `duplicates` includes cross-file fingerprint reviews; identical `file_hash` replays.
