# Research

- Post-login **workspace cards** when multi-org; auto-open if exactly one (optional — user asked for cards + remember, so show cards unless remembered).
- Keep **tenant context in header and sidebar**, not only a cookie.
- Persist org id in local storage; clear on sign-out / missing membership.
- Shell: dark header + light sidebar (Zoho pattern); ambient org name always visible.
